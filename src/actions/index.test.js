import * as actions from './index';

describe('actions', () => {
  describe('addLocation', () => {
    it('should return false', () => {
      expect(true).toEqual(false)
    })

    it('should return a type of ADD_LOCATION with a single location', () => {
      const location = {};

    })
  })
})